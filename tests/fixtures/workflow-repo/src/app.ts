/**
 * Responsibility: Fixture entry module.
 * Scope: Exercise imports, symbols, and TODO extraction.
 */
import { routeWork } from "./core/router.ts";
import "./styles/app.css";

// TODO: replace the optimistic fixture path with a real workflow plan
export function runApp(input) {
  return routeWork(input);
}
