{
  "id": "execute-ticket",
  "summary": "Plan or execute a single ticket with graph-backed context, guarded mutation, and verification evidence.",
  "stability": "experimental",
  "category": "execution",
  "runner": "node-script",
  "entry": "aiwf-shell/scripts/ai-workflow/execute-ticket.ts",
  "focus": "ticket implementation, code repair, code review, and verification",
  "taskClass": "feature-implementation",
  "inputSchema": {
    "type": "object",
    "required": ["ticketId"],
    "properties": {
      "ticketId": { "type": "string" },
      "apply": { "type": "boolean" },
      "timeoutMs": { "type": "number" },
      "mode": { "type": "string" },
      "root": { "type": "string" },
      "evidenceRoot": { "type": "string" },
      "allowExternalTarget": { "type": "boolean" }
    }
  },
  "outputSchema": {
    "type": "object",
    "required": ["success", "ticketId", "verification", "runArtifact"],
    "properties": {
      "success": { "type": "boolean" },
      "ticketId": { "type": "string" },
      "verification": { "type": "object" },
      "runArtifact": { "type": "object" }
    }
  },
  "contextPolicy": {
    "mode": "graph-backed-surgical",
    "requiresTicket": true,
    "include": ["ticket", "workingSetEvidence", "guidelines", "verificationPlan"]
  },
  "toolPolicy": {
    "canWriteFiles": true,
    "requiresApplyFlag": true,
    "requiresSafeRepairTarget": true,
    "requiresVerification": true
  },
  "graderId": "ticket-execution-v1",
  "maxRetries": 1,
  "canMutate": true
}
